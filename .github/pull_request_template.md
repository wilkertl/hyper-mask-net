## Summary

Closes #

## Checklist (plan §9.3)

1. **Contract changed:** none / describe the schema or public API change
2. **Files changed:**
3. **Tests added:**
4. **Commands run:**

   ```bash
   ruff check . && ruff format --check . && mypy && pytest
   ```

5. **Expected artifacts or regressions:**
6. **Changes the scientific protocol?** no / yes — add the `protocol-change` label and explain
7. **Invalidates previous results?** no / yes — add the `invalidates-results` label and link the affected results
